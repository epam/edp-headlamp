import makeStyles from '@mui/styles/makeStyles';

export const useStyles = makeStyles(() => ({
  cardRoot: {
    width: '100%',
    height: '100%',
    position: 'relative',
    backgroundColor: '#f9f9f9',
    borderRadius: '4px',
  },
}));
