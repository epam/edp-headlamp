import makeStyles from '@mui/styles/makeStyles';

export const useStyles = makeStyles(() => ({
  treeItemTitle: {
    whiteSpace: 'nowrap',
    textOverflow: 'ellipsis',
    overflow: 'hidden',
  },
}));
