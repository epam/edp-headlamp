export const FORM_NAMES = {
  SHARED: 'shared',
  GIT_SERVER: 'gitServer',
  CREDENTIALS: 'credentials',
} as const;

export const GIT_USER = {
  GERRIT: 'edp-ci',
  GITHUB: 'git',
  GITLAB: 'git',
};
