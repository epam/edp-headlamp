apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "edp-headlamp.fullname" . }}-test-connection"
  labels:
    {{- include "edp-headlamp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "edp-headlamp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
