apiVersion: v1
kind: Service
metadata:
  name: {{ include "edp-headlamp.fullname" . }}
  labels:
    {{- include "edp-headlamp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "edp-headlamp.selectorLabels" . | nindent 4 }}
